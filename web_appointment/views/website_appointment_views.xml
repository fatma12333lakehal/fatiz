<odoo>

    <!-- ========================================= -->
    <!-- TREE VIEW -->
    <!-- ========================================= -->

    <record id="view_appointment_tree" model="ir.ui.view">
        <field name="name">website.appointment.list</field>
        <field name="model">website.appointment</field>
        <field name="arch" type="xml">

            <list string="Appointments">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="employee_id"/>
                <field name="date"/>
                <field name="time"/>
                <field name="location"/>
                <field name="state"/>
            </list>

        </field>
    </record>

    <!-- ========================================= -->
    <!-- FORM VIEW -->
    <!-- ========================================= -->

    <record id="view_appointment_form" model="ir.ui.view">
        <field name="name">website.appointment.form</field>
        <field name="model">website.appointment</field>
        <field name="arch" type="xml">

            <form string="Appointment">

                <header>

                    <button name="action_confirm"
                            type="object"
                            string="Confirm"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_done"
                            type="object"
                            string="Done"
                            class="btn-success"
                            invisible="state != 'confirmed'"/>

                    <button name="action_cancel"
                            type="object"
                            string="Cancel"
                            class="btn-danger"
                            invisible="state == 'cancel'"/>

                    <field name="state" widget="statusbar"/>

                </header>

                <sheet>

                    <group>

                        <group>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="employee_id"/>
                            <field name="project_id"/>
                        </group>

                        <group>
                            <field name="date"/>
                            <field name="time"/>
                            <field name="duration"/>
                            <field name="location"/>
                        </group>

                    </group>



                </sheet>

            </form>

        </field>
    </record>

    <!-- ========================================= -->
    <!-- SEARCH VIEW -->
    <!-- ========================================= -->

    <!-- ========================================= -->
    <!-- ACTION -->
    <!-- ========================================= -->

    <record id="action_appointments" model="ir.actions.act_window">
        <field name="name">Appointments</field>
        <field name="res_model">website.appointment</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ========================================= -->
    <!-- MENUS -->
    <!-- ========================================= -->

    <menuitem id="appointment_root_menu"
              name="Appointments"
              sequence="10"/>

    <menuitem id="appointment_menu"
              name="Bookings"
              parent="appointment_root_menu"
              action="action_appointments"
              sequence="10"/>

</odoo>